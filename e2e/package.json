{
  "name": "techspar-e2e-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:single": "playwright test tests/login.spec.js --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
